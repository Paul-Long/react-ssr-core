import { fontColor } from './color';

export const yAxisValue = {
  type: 'value',
  scale: true,
  axisLabel: {
    color: fontColor
  },
};

export const yAxisCategory = {
  type: 'category',
  scale: true,
  axisLabel: {
    color: fontColor
  },
};